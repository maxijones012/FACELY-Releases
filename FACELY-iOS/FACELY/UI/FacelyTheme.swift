import SwiftUI

enum FacelyTheme {
    static let accent = Color(red: 0.15, green: 0.85, blue: 0.95)
    static let surface = Color.white.opacity(0.06)
    static let border = Color.white.opacity(0.10)
}
